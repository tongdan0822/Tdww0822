# 急诊抢救护理记录系统 - 公网服务器部署指南

> 不需要同一WiFi！手机用4G/5G、电脑用医院内网，都能通过公网服务器实时同步数据。

---

## 方案A：Render免费云平台部署（推荐，0元/月，5分钟搞定）

### 第一步：注册Render账号
1. 打开 https://render.com
2. 点击右上角「Sign Up」
3. 用GitHub账号登录（没有GitHub就先注册一个，免费的）
4. 登录后进入Render控制台

### 第二步：上传代码到GitHub
1. 打开 https://github.com/new 创建一个新仓库（Repository）
2. 仓库名随便填，比如 `emergency-nursing`
3. 选择「Public」（公开，免费）
4. 点击「Create repository」
5. 把本系统压缩包解压后，全部文件上传到这个GitHub仓库
   - 可以直接在GitHub网页点「Add file」→「Upload files」上传
   - 确保 `render.yaml` 文件在仓库根目录

### 第三步：一键部署到Render
1. 在Render控制台，点击「New +」→「Blueprint」
2. 选择你刚才创建的GitHub仓库
3. Render会自动读取 `render.yaml` 配置
4. 点击「Apply」开始部署
5. 等待2-3分钟，部署完成后会显示一个网址，类似：
   `https://emergency-nursing-record.onrender.com`
   **这个就是你的公网服务器地址！**

### 第四步：初始化账号
1. 浏览器打开上面的公网地址
2. 点击「注册」，创建你的账号：
   - 读写账号：用户名 `舒兰市人民医院急诊`，密码 `shulan120`
   - 只读账号：用户名 `舒兰市人民医院急诊-查看`，密码 `shulan120TD`
3. 注册成功后即可登录使用

### 第五步：各端配置
- **电脑网页端**：直接浏览器打开公网地址，登录即可
- **手机APP端**：安装APK，服务器地址输入公网地址（如 `https://emergency-nursing-record.onrender.com`），登录即可
- **手机网页端**：手机浏览器打开公网地址，登录即可

> ✅ 现在手机用4G/5G、电脑用医院内网，都能同步数据了！

---

## 方案B：购买云服务器（数据更可控，约30-50元/月）

适合对数据安全要求更高的场景，数据完全存在自己的服务器上。

### 推荐云服务商
- 阿里云：https://www.aliyun.com （轻量应用服务器，约30元/月）
- 腾讯云：https://cloud.tencent.com （轻量应用服务器，约30元/月）
- 华为云：https://www.huaweicloud.com

### 部署步骤
1. 购买一台最低配置的云服务器（1核2G足够），操作系统选 Ubuntu 22.04
2. 在服务器安全组放行 3000 端口
3. 用SSH连接服务器，安装Node.js：
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```
4. 把系统文件上传到服务器（可以用FileZilla等工具）
5. 进入server目录，安装依赖并启动：
   ```bash
   cd emergency-nursing-sync/server
   npm install
   nohup node server.js &
   ```
6. 服务器公网IP就是你的访问地址，如 `http://你的服务器IP:3000`

---

## 方案C：内网穿透（临时使用，免费但不稳定）

如果暂时不想买服务器，可以用内网穿透工具把家里/办公室的电脑暴露到公网。

### 推荐工具
- 花生壳：https://hsk.oray.com （有免费版）
- ngrok：https://ngrok.com （有免费版）
- cpolar：https://www.cpolar.com （有免费版）

### 使用步骤（以花生壳为例）
1. 下载安装花生壳客户端
2. 注册登录，添加一个HTTP映射
3. 内网主机填 `127.0.0.1`，端口填 `3000`
4. 启动映射，会得到一个公网域名
5. 这个公网域名就是你的服务器地址

> ⚠️ 注意：免费版内网穿透速度慢、不稳定，适合临时测试，不建议长期正式使用。

---

## 常见问题

### Q：Render免费版有什么限制？
A：免费版15分钟无访问会自动休眠，下次访问需要等几十秒唤醒。数据存在1GB持久化磁盘上，不会丢失。如果觉得休眠麻烦，可以升级到付费版（约7美元/月）。

### Q：数据安全吗？
A：Render是正规美国云服务商，数据加密存储。如果对数据安全要求极高，建议用方案B购买国内云服务器，数据完全自己掌控。

### Q：医院内网能访问外网吗？
A：一般医院电脑都能访问外网（能打开百度就行）。如果医院有严格的网络限制，只能访问内网，那就需要用方案B把服务器部署在医院内网。

### Q：APP里服务器地址怎么填？
A：填完整的公网地址，包括 `https://` 或 `http://`，例如：
- Render：`https://emergency-nursing-record.onrender.com`
- 云服务器：`http://123.45.67.89:3000`
- 内网穿透：`https://xxx.vip.cpolar.cn`

### Q：怎么导出Excel？
A：在电脑网页端打开任意患者记录，点击「导出Excel」按钮，会自动下载.xlsx文件，可直接打印。

---

## 技术支持
部署过程中遇到问题，可以随时咨询。
